# Bell Performance 13.7.14 — Engine Session Alias Repair

- Resolves abbreviated scheduled engine missions such as `R-1` and `R-2` to their full workout-library keys before preview or launch.
- Fixes Engine Preview and Start Engine on plans produced by the adaptive weekly scheduler.
- Preserves plan/session identifiers so completion is still recorded against the correct calendar session.
